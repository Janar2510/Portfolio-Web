// Shared constants

export const SUPPORTED_LOCALES = ['en', 'et'] as const;
export const DEFAULT_LOCALE = 'en' as const;

export const PROJECT_STATUSES = [
  'planning',
  'active',
  'on-hold',
  'completed',
  'archived',
] as const;

export const EMAIL_PROVIDERS = ['outlook', 'apple'] as const;
