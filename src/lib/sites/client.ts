// Client-side site helpers (optional for now)
// Most site operations should be done via server actions or API routes for security

export const sitesClient = {
    // Add client-side wrappers here if needed later
};
